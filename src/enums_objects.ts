enum Roles {
    User = "USER",
    Admin = "ADMIN",
    SuperAdmin = "SUPER_ADMIN"
}

console.log(Roles.Admin);

// objects
const roles = {
    User: 0,
    Admin: 1,
    SuperAdmin: 2
};

console.log(roles.User);